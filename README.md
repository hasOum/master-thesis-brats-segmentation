# master-thesis-brats-segmentation

   # Brain Tumour Segmentation with Physical Regularisation
   
   This repository contains the Python implementation of the two segmentation pipelines (supervised and unsupervised) developed for the Master's thesis of Oumama Hassani.
   
   ## Requirements
   Install the required packages:
   ```bash
   pip install -r "requirements Pipeline_A.txt"
   pip install -r "requirements Pipeline_B.txt"
